<%- include("./partial/head")  %> 

    <header class="header">
        <div class="header__block">
            <h1 class="header__block-title">Liste des lieux</h1>
        </div>
        <%- include("./component/nav")  %>
    </header>

    <main class="main__product">
        <div class="main__product-list">
            <% places.forEach(place => { %>
                <p><%= place.name %></p>
            <% }); %>
        </div>
    </main>

    <footer class="footer">
        <p>Orvoir</p>
    </footer>

<%- include("./partial/footer")  %> 